# Evaluation config for In-the-Wild dataset
# Usage: python -m sls_asvspoof.train --config configs/eval_wild.yaml --model_path <path> --eval_output <path>

track: In-the-Wild
is_eval: true
eval: true
eval_batch_size: 4
protocols_path: keys/in_the_wild_filelist.txt
# database_path: /path/to/release_in_the_wild/  # set via CLI
xlsr_model: xlsr2_300m.pt
